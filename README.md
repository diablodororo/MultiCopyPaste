# MultiCopyPaste 🚀
**跨平台序列循環剪貼簿小工具 (macOS / Windows)**
*[English Version](README.en.md)*

---

## 💡 痛點與為此而生的解決方案

在日常文書處理、試算表搬移或填寫線上表單時，我們經常需要重複：
`複製A -> 貼上A -> 複製B -> 貼上B -> 複製C -> 貼上C...`

**MultiCopyPaste** 讓您可以在來源視窗**一口氣依序複製 N 筆資料 (A, B, C)**，接著在目標視窗連續按下 **全域貼上快捷鍵**，工具就會依序將 `A -> B -> C -> A -> B -> C` 自動循環貼上！

---

## ✨ 產品特色

- ⚡ **極致輕量與低耗能**：基於 Tauri v2 + Rust 打造，背景常駐記憶體僅約 18~25 MB。
- 🌐 **雙語介面 (i18n)**：右上角一鍵即時切換繁體中文與 English 介面。
- 🎹 **全域快捷鍵**：按下 `Ctrl + Option + V` (macOS) 或 `Ctrl + Alt + V` (Windows) 即可瞬間依序貼上。
- 🛡️ **穩定防當機制**：macOS 採用 Apple 原生 CoreGraphics `CGEvent` 與無鎖佇列，保證高頻操作 100% 執行緒安全不當機。
- 🌟 **系統選單列常駐與快選單 (System Tray)**：macOS 頂部選單列採用專為 Template 模式打造的純粹幾何透明圖示（自動適應深色/淺色選單列主題），左鍵點擊即可呼叫主視窗或離開應用。
- 🎨 **現代品牌視覺與高質感 UI**：絕美暗黑玻璃擬態主視窗，搭配專屬客製化魔法棒循環複製 App Icon，即時預覽當前序列與 `NEXT` 指標。

---

## ⌨️ 快捷鍵與操作說明

| 操作 | 預設快捷鍵 |
| :--- | :--- |
| **依序循環貼上** | `Ctrl + Option + V` (macOS) / `Ctrl + Alt + V` (Windows) |
| **重置循環指標** | 可於介面上點擊「重置回頂部」按鈕 |
| **呼叫與隱藏視窗** | 左鍵點擊頂部選單列圖示 -> 選擇「顯示視窗」或關閉視窗常駐後台 |

---

## 📦 更改紀錄 (Changelog)

### v0.0.5 (當前版本)
- 🎨 **專屬品牌圖示全面升級**：更新主應用程式與 DMG 安裝包的 App Icon 為魔杖與字母排版視覺。
- 🌟 **新增系統選單列 (Menu Bar) 極簡線條圖示**：獨立裁剪純白透明幾何圖示並啟用 `icon_as_template(true)`，完美對應 macOS 淺色/深色主題自動轉換。
- 🚀 **底層建置流程優化**：改寫 `.dmg` 打包機制，於背景純靜態壓縮封裝，不再開啟或干擾 Finder GUI 視窗。

### v0.0.4
- 🌐 **雙語語系切換 (i18n)**：新增繁體中文與英文雙語切換選單與完整版本號資訊列。
- 🖥️ **選單列常駐功能**：支援視窗關閉時隱藏至頂部選單列並透過左鍵快選單叫出。

### v0.0.3
- 🎹 **全域快捷鍵修改**：修改預設連續貼上快捷鍵為 `Ctrl + Option + V` (macOS)。
- 🛡️ **修復連續貼上當機問題**：重構 CoreGraphics `CGEvent` 與剪貼簿事件監聽執行緒，解決 SIGTRAP / 記憶體非法存取問題。

---

## 🛠️ 開發與建置

### 前置需求
- Node.js (>= 18)
- Rust (>= 1.77)

### 啟動開發伺服器
```bash
npm install
npx tauri dev
```

### 打包正式版應用程式 (Build App / DMG)
```bash
npm run build && npx tauri build
```
打包完成後產物將放置於 `src-tauri/target/release/bundle/dmg/` 目錄中。

---

## 📄 授權條款
MIT License
